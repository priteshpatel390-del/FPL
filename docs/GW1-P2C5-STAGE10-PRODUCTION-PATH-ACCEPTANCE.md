# GW1-P2C5 — Stage 10 production-path acceptance candidate

Status: **implementation candidate only — physical acceptance has not happened; candidate is not merged.**

GW1-P2C5 adds one deterministic infrastructure-only `preDeadlineSnapshot` in the unmistakably synthetic `2099-00` / GW38 namespace. It is incomplete, client-recorded, non-Official, and has every approved provider disabled and excluded from model use. It contains no genuine FPL manager, squad, league, provider payload, player projection, recommendation or historic GW1 evidence. It is never genuine prospective evidence and can never establish Official eligibility.

The candidate control is absent from ordinary navigation. It is constructed only on the exact production application origin, the exact deadline-evidence route, and an explicit `stage10Acceptance=1` query gate, after the generated fixture reproduces its predeclared immutable identity. It stores through the unchanged local Stage 10 storage path. The normal `teamsheet:evidence-stored` listener then enqueues and schedules the unchanged browser delivery service; the action does not call the archive or `flushOutbox()` directly.

The only foreign-season outbox admission is the exact immutable fixture identity: season `2099-00`, GW38, local-capture outbox/schema compatibility fields, snapshot ID `predeadline-gw38-ae9149da8e77faaa`, and content hash `ae9149da8e77faaaf1f25400310de7f5c2e7a1c2f20c9645e7ba99a9fa3c4408`. Arbitrary records in `2099-00` and every other foreign season remain excluded. The expected private R2 key is `evidence/v1/preDeadlineSnapshot/2099-00/gw38/ae9149da8e77faaaf1f25400310de7f5c2e7a1c2f20c9645e7ba99a9fa3c4408.json.gz`.

No Worker source, configuration, validator, archive endpoint, Stage 10 timing rule, provider rule, retention rule, model or recommendation behaviour changes. The existing Worker validator accepts the fixture unchanged.

Natural deadline capture remains unproven by this fixture. Historic GW1 recovery remains parked and non-blocking. Physical iPhone Safari execution, a real Access session, live R2/D1 custody, receipt state and browser terminal acknowledgement all remain future owner-approved acceptance steps. Passing repository verification does not approve deployment or execution.
