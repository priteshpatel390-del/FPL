name: Apply Leagues standings empty state

on:
  push:
    branches:
      - agent/leagues-standings-preseason-empty-state

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm helper can run
        run: echo helper-ready
