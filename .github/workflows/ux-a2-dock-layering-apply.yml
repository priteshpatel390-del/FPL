name: UX-A2 dock layering verifier

on:
  push:
    branches:
      - agent/ux-a2-player-detail-dock-layering

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "UX-A2 verifier started"
