name: Stage 9.2 closeout

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  closeout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/stage9-2-pitch-shirts
          fetch-depth: 0
      - name: Record Stage 9.2 and verify committed deployables
        run: |
          python - <<'PY'
          from pathlib import Path

          source='02d59e6e6bbdc0fe9480642d717bfcfa4f9367ad'
          artefacts='372576324b049972b88437540046c3c1f85457d5'

          p=Path('CLAUDE.md'); s=p.read_text()
          old="Stage 8 is merged into `main` at `b0f6e1683b1bdb38c9cb233024a8d0bfd652caa8` with a verified **284/284** baseline. Stage 9 — UI integration is approved in `docs/STAGE9-DESIGN.md`. Checkpoint 9.1 is implemented and verified on draft PR #17 from branch `agent/stage9-1-app-shell`: four primary destinations (Team, Players, Transfers and More), Team as the default landing screen, and the existing setup/fixtures/league/Ask surfaces regrouped under More. No model or feature behaviour changed. Owner review and explicit merge approval remain required. Checkpoint 9.2 must not begin before 9.1 is merged. Uncertainty spread-label thresholds remain separately gated for owner approval during checkpoint 9.3."
          new=f"Stage 9.1 is merged into `main` through PR #17 at `9f4333e3f2e8d71d02355389f8c7d2115d3d17e4`. Checkpoint 9.2 — Team pitch and shirts is implemented and verified on draft PR #18 from branch `agent/stage9-2-pitch-shirts`: portrait model-selected XI, repository-owned CSS shirts, clear captain/vice treatment and preserved bench order. Verified source `{source}` passes **288/288** tests with deterministic builds; generated artefacts are committed at `{artefacts}`. No model, provider, simulation or optimiser behaviour changed. Owner review and explicit merge approval remain required. Checkpoint 9.3 must not begin before 9.2 is merged, and its descriptive uncertainty spread-label thresholds remain separately gated for owner approval."
          assert old in s; p.write_text(s.replace(old,new))

          p=Path('docs/PROJECT_CONTEXT.md'); s=p.read_text()
          s=s.replace("- Stages 1–7 are complete and merged.\n- Stage 7 merged through PR #15 at `78b42e09d2ad3b8338b587ba2b89e815cfc0c45a` with a verified baseline of 274 passing tests, successful build, deterministic two-build comparison and build-identity checks.\n- Stage 8 — Uncertainty and Squad Simulation is implemented on draft PR #16 and remains behind full verification and owner-review gates.",
          "- Stages 1–8 and Stage 9 checkpoint 9.1 are complete and merged.\n- Stage 9.1 merged through PR #17 at `9f4333e3f2e8d71d02355389f8c7d2115d3d17e4`, establishing Team, Players, Transfers and More with Team as the default landing screen.\n- Stage 9.2 — Team pitch and shirts is implemented and verified on draft PR #18 with 288 passing tests, successful deterministic builds and owner review still required.")
          s=s.replace("Completed: Stage 1 characterisation/audit, SEC-1, Stage 2 modularisation, repository handover, Stage 3 security/provider hardening, Stage 4 expected minutes, Stage 5 scoring corrections, Stage 6 transfer optimiser and Stage 7 walk-forward backtest.\n\nCurrent: Stage 8 implementation and verification on draft PR #16. It adds a deterministic uncertainty layer, percentiles, threshold probabilities, legal squad auto-subs and captain/vice fallback without changing deterministic `projectXP()`.\n\nNext after Stage 8 merge: Stage 9 UI integration.",
          "Completed: Stage 1 characterisation/audit, SEC-1, Stage 2 modularisation, repository handover, Stage 3 security/provider hardening, Stage 4 expected minutes, Stage 5 scoring corrections, Stage 6 transfer optimiser, Stage 7 walk-forward backtest, Stage 8 uncertainty/squad simulation and Stage 9.1 app shell.\n\nCurrent: Stage 9.2 implementation and owner review on draft PR #18. It presents the unchanged model-selected XI on a portrait pitch with repository-owned CSS shirts and clear model-selected captain/vice treatment.\n\nNext after Stage 9.2 merge: Stage 9.3 player detail and uncertainty presentation. Descriptive spread-label thresholds require separate owner approval before implementation.")
          p.write_text(s)

          p=Path('docs/ARCHITECTURE.md'); s=p.read_text()
          s=s.replace("  ui/                    views, restricted Markdown and security wiring", "  ui/                    app shell, visual-only team-pitch helpers, views, restricted Markdown and security wiring")
          s=s.replace("The bundler flattens application modules in a fixed, explicit order. Stage 8 modules are bundled after deterministic scoring and before downstream squad/transfer consumers. Direct ES imports remain the source/test contract. The build rejects surviving module syntax and requires unique top-level names in the flattened scope.",
          "The bundler flattens application modules in a fixed, explicit order. Stage 8 modules are bundled after deterministic scoring and before downstream squad/transfer consumers. `ui/team-pitch.mjs` is bundled after the Stage 9 app shell and before the main views so its pure visual helpers are available without owning selection logic. Direct ES imports remain the source/test contract. The build rejects surviving module syntax and requires unique top-level names in the flattened scope.")
          marker="## Provider Health and storage"
          insert="""## Stage 9 UI boundary
`ui/app-shell.mjs` owns the four primary destinations. `ui/team-pitch.mjs` owns only deterministic visual grouping and repository-owned shirt palettes. `ui/views.mjs` continues to call the existing `bestXI()` and projection functions, then renders those unchanged results onto the portrait pitch. Captain and vice badges represent the first two distinct players from the existing captain ranking; no formula or persisted squad state is changed.

"""
          assert marker in s; s=s.replace(marker,insert+marker)
          s=s.replace("Stage 8 adds player-simulation and squad-simulation suites.", "Stage 8 adds player-simulation and squad-simulation suites. Stage 9.2 adds direct visual-helper tests for formation grouping, captain/vice identity and deterministic shirt palettes.")
          p.write_text(s)

          p=Path('docs/ROADMAP.md'); s=p.read_text()
          s=s.replace("- **Stage 8 — Uncertainty and squad simulation** · DONE and merged through PR #16 at `b0f6e1683b1bdb38c9cb233024a8d0bfd652caa8`.\n  - Deterministic seeded player and legal squad simulations.\n  - P10/P25/median/P75/P90 plus blank/return/haul probabilities.\n  - Reserve-goalkeeper handling, ordered legal auto-subs and captain-to-vice fallback.\n  - Verified baseline: **284/284 tests passed**, successful build and deterministic build checks.",
          "- **Stage 8 — Uncertainty and squad simulation** · DONE and merged through PR #16 at `b0f6e1683b1bdb38c9cb233024a8d0bfd652caa8`.\n  - Deterministic seeded player and legal squad simulations.\n  - P10/P25/median/P75/P90 plus blank/return/haul probabilities.\n  - Reserve-goalkeeper handling, ordered legal auto-subs and captain-to-vice fallback.\n  - Verified baseline: **284/284 tests passed**, successful build and deterministic build checks.\n- **Checkpoint 9.1 — App shell and primary navigation** · DONE and merged through PR #17 at `9f4333e3f2e8d71d02355389f8c7d2115d3d17e4`.\n  - Four primary destinations: Team, Players, Transfers and More.\n  - Team is the default landing screen; existing supporting tools are regrouped under More.\n  - Verified baseline: **284/284 tests passed**, successful build and deterministic build checks.")
          start=s.index('## Current')
          s=s[:start]+f"""## Current
- **Stage 9 — UI integration** · approved design in `docs/STAGE9-DESIGN.md`.
- **Checkpoint 9.2 — Team pitch and shirts** · implemented and verified on draft PR #18; owner review and explicit merge approval remain.
  - Portrait football pitch presents the unchanged model-selected XI.
  - Repository-owned CSS shirts use deterministic club palettes with safe fallbacks.
  - Model-ranked captain and vice-captain are marked directly on the pitch; bench order is preserved.
  - Verified source commit `{source}`: **288/288 tests passed**, successful build, deterministic two-build comparison and build-identity check.
  - Generated deployables committed at `{artefacts}`.
  - No projection, minutes, scoring, fixture, simulation, squad, optimiser or provider behaviour changed.

## Upcoming
1. **Checkpoint 9.3 — Player detail and uncertainty** · only after 9.2 is merged; spread-label thresholds require separate owner approval.
2. **Checkpoint 9.4 — Decision previews** · temporary captain/vice and transfer-plan previews.
3. **Checkpoint 9.5 — Settings and Provider Health integration**.
4. **Checkpoint 9.6 — Inline-style migration, CSP tightening and final UI polish**.

## Current blockers
1. Owner review and explicit merge approval for Stage 9.2 draft PR #18.
2. Descriptive uncertainty spread-label thresholds remain unapproved for checkpoint 9.3.
3. Free historical odds and other missing pre-deadline provider snapshots require prospective 2026/27 logging.
4. Live-season prospective data requirements begin with GW1 on 21 August 2026.
"""
          p.write_text(s)

          p=Path('docs/STAGE9-DESIGN.md'); s=p.read_text()
          needle="### 9.2 — Team pitch and shirts\n\n- Replace the current squad presentation with the approved portrait pitch.\n- Add repository-owned CSS shirt representations and captain/vice treatment.\n- Keep the existing model-selected XI and squad logic unchanged."
          replacement=needle+f"\n\nStatus: **Implemented and verified on draft PR #18.** Source `{source}` passes 288/288 tests; generated artefacts are at `{artefacts}`. Owner review and merge approval remain required."
          assert needle in s; p.write_text(s.replace(needle,replacement))

          p=Path('docs/CHANGELOG.md'); s=p.read_text()
          marker='## [Stage 7]'
          entry=f"""## [Stage 9.2] — 2026-07-28 — Team pitch and shirts
### Added
- Portrait, mobile-first football pitch for the unchanged model-selected starting XI.
- Repository-owned CSS shirt shapes and deterministic club palettes with safe fallbacks.
- Direct captain and vice-captain badges, compact fixture/xP cards and preserved bench order.
- Four focused tests for formation grouping, captaincy identity and shirt-palette determinism.
### Changed
- The Team surface now leads with projected score, shape, squad value, free transfers and the football-first pitch rather than legacy flat player cards.
- The existing captain ranking and six-Gameweek squad table remain available below the pitch.
### Verification
- Verified source commit `{source}`.
- Full suite: **288/288 passing**.
- Production build succeeded; deterministic two-build comparison and exact build-identity checks passed.
- Verified generated artefacts committed at `{artefacts}`.
### Unchanged
- `bestXI()`, bench ordering, projections, expected minutes, scoring, fixtures, simulation, captain ranking, transfer optimisation, providers, storage and security behaviour.
- No official FPL or club artwork and no prediction-accuracy claim.
### Status
- Implemented and verified on draft PR #18; awaiting owner review and explicit merge approval.

## [Stage 9.1] — 2026-07-28 — App shell and primary navigation
### Added
- Four primary destinations: Team, Players, Transfers and More, with Team as the default landing screen.
### Status
- Merged through PR #17 at `9f4333e3f2e8d71d02355389f8c7d2115d3d17e4`.

"""
          assert marker in s; p.write_text(s.replace(marker,entry+marker))

          p=Path('docs/STAGE_HISTORY.md'); s=p.read_text()
          s=s.replace('STAGE7-DESIGN.md, AUDIT.md.', 'STAGE7-DESIGN.md, STAGE8-DESIGN.md, STAGE9-DESIGN.md, AUDIT.md.')
          marker='## Stage 7 — Walk-forward backtest'
          entry=f"""## Stage 9.2 — Team pitch and shirts (IMPLEMENTED AND VERIFIED 2026-07-28; awaiting merge)
Replaced the legacy flat Best XI cards with a portrait, mobile-first football pitch while leaving `bestXI()`, projection calls and bench order unchanged. The same captain ranking now supplies direct captain and vice-captain badges. Compact cards show repository-owned CSS shirts, player names, next fixture and next-Gameweek expected points; injury/doubt warnings remain visible.

A visual-only `src/ui/team-pitch.mjs` boundary owns formation grouping and deterministic club-colour palettes, including repository-owned fallbacks for unknown teams. No official FPL or club artwork is used. Four focused tests raise the suite from 284 to 288. Verified source `{source}` passed **288/288**, production build and byte-for-byte repeat build checks; generated artefacts are committed at `{artefacts}`. No model, provider, simulation, optimiser, storage or security behaviour changed. Draft PR #18 awaits owner review and explicit merge approval.

## Stage 9.1 — App shell and primary navigation (MERGED 2026-07-28)
Established Team, Players, Transfers and More, made Team the default landing screen and regrouped existing supporting surfaces under More without changing feature behaviour. Verified at 284/284 tests and merged through PR #17 at `9f4333e3f2e8d71d02355389f8c7d2115d3d17e4`.

"""
          assert marker in s; p.write_text(s.replace(marker,entry+marker))
          PY

          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add CLAUDE.md docs/PROJECT_CONTEXT.md docs/ARCHITECTURE.md docs/ROADMAP.md docs/STAGE9-DESIGN.md docs/CHANGELOG.md docs/STAGE_HISTORY.md
          git commit -m 'Record Stage 9.2 verification status'

          SOURCE_SHA=02d59e6e6bbdc0fe9480642d717bfcfa4f9367ad
          mkdir -p /tmp/committed-dist /tmp/first-build
          cp dist/index.html dist/app.bundle.js dist/manifest.json /tmp/committed-dist/
          BUILD_COMMIT="$SOURCE_SHA" ./run-tests.sh
          cmp /tmp/committed-dist/index.html dist/index.html
          cmp /tmp/committed-dist/app.bundle.js dist/app.bundle.js
          cmp /tmp/committed-dist/manifest.json dist/manifest.json
          cp dist/index.html dist/app.bundle.js dist/manifest.json /tmp/first-build/
          BUILD_COMMIT="$SOURCE_SHA" node build.mjs
          cmp /tmp/first-build/index.html dist/index.html
          cmp /tmp/first-build/app.bundle.js dist/app.bundle.js
          cmp /tmp/first-build/manifest.json dist/manifest.json
          node -e "const m=require('./dist/manifest.json');if(m.commit!=='$SOURCE_SHA')throw new Error('build identity mismatch: '+m.commit)"
          git diff --exit-code -- dist/
          git push origin HEAD:agent/stage9-2-pitch-shirts
